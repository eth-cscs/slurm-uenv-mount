name: Release plugin RPM

on:
  workflow_dispatch:

jobs:
  rpm:
    runs-on: ubuntu-latest
    container: ghcr.io/eth-cscs/slurm-container-test:latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: lsb_release && ls && pwd
