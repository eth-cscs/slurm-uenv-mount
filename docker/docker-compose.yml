services:
  slurm:
    privileged: true
    container_name: slurm-uenv-mount
    volumes:
      - ../:/slurm-uenv-mount:ro
    build:
      context: ../
      dockerfile: docker/Dockerfile
    command: tail -F anything
